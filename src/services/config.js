//const
const TIMEOUT = 1000;  // timeout setting in ms
const BASEURL = 'https://reservation-api.beije.it/' //! baseURL for json server. REMEMBER TO SET IT ON PORT 5000! command: json-server --watch "PATH JSON FILE" --port 5000 


export {
    TIMEOUT,
    BASEURL
}